x = y = 1;

c = 10;
count = function(current) {
    c = (c - 1);
};

# for i in [1,1,1,1,1,1,1,1,1] {
while count(99) and false {
    c = c + 1;
    h = y;
    if h {
        f = (y = (x + y));
        if f {
            g = (x = h);
            if g {
                print(x);
                ~f;
                ~g;
                ~h;
            };
        };
    };
};
